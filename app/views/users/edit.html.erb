<% present @user do |presenter| %>

<%= title edit(:profile) %>

<hr>

<%= simple_form_for @user do |f| %>
  <%= f.input :userid %>
  <%= f.input :image %>
  <%= f.button :submit, class:'submit' %>
<% end %>

<%# presenter.cancel_button(session_month, t('helpers.submit.calendar')) %>

<% end %>
