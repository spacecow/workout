<% present post do |presenter| %>
  <div class="post">
    <%= presenter.training_type %>
    <%= presenter.timestamp %>
    <%= presenter.author %>
    <%= presenter.training_partners %>
    <%= presenter.actions(date,month) %>
    <div class="clear"></div>
    <%= presenter.comment %>
  </div>
  <hr>
<% end %>
