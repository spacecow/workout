<% present post do |presenter| %>
  <hr>
  <li class="post thumb" style="background-color: <%= post.intensity_colour %>">
    <div class="left relative" style="color: <%= post.intensity_colour %>">
      <%= presenter.image %>
      <div class="number">
        <%= post.intensity %>
      </div>
    </div>
    <div class="right">
      <%= presenter.title(post.training_types,title) %>
      <%= presenter.timestamp %>
      <%= presenter.distance %>
      <%= presenter.author %>
      <%= presenter.training_partners %>
      <%= presenter.actions("new_comment_#{i}") %>
      <div class="clear"></div>
      <%= presenter.comment %>
      <%= presenter.comments %>
      <%# render 'comments/comments', comments:comments, background:post.intensity_colour, delete:true if comments.present? %>
      <%= simple_form_for [post, comment], html:{id:"new_comment_#{i}"} do |f| %>
        <%= f.input :content, label:false %>
        <%= f.button :submit, class: :submit %>
      <% end %>
    </div>
    <div class="clear"></div>
  </li>
<% end %>

